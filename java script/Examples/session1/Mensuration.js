/**
 * 
 */

function Mensuration(breadth, height)
{
	var area=function()
	{
		var area = breadth * height;
		alert("Area of "+ breadth + " and"+ height +" is "+ area );
	}
	var perim=function()
	{
		var area = 2 (breadth + height);
		alert("Area of "+ breadth + " and"+ height +" is "+ area );
	}
}